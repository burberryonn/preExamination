const fs = require('fs').promises;
const fetchUserById = require('./fetchUserById');

function getFilesFromFolder(pathToFolder) {}

function readOneFile(pathToFile) {}

function getUsersArray(pathToFile) {}

module.exports = {
  getFilesFromFolder,
  readOneFile,
  getUsersArray,
};
